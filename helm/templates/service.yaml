apiVersion: v1
kind: Service
metadata:
  name: { { include "harz-storage.fullname" . } }
  labels: { { - include "harz-storage.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "harz-storage.selectorLabels" . | nindent 4 } }
